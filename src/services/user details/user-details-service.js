const { StatusCodes } = require('http-status-codes');
const AppError = require('../../utils/errors/app-error');
const { UserDetailsRepository } = require('../../repositories');

const userRepo = new UserDetailsRepository();


  

module.exports = {

}